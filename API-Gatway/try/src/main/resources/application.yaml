spring:
  application:
    name: try
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XE
    username: C##TRY
    password: 123
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      # As we discussed, 'update' is perfect for your development phase!
      ddl-auto: update
    show-sql: true # Optional: This helps you see the SQL queries in your console
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
server:
  port: 8080
  servlet:
    context-path: /api